# 🚗 Real-time Traffic Analysis System

Програма для автоматичного аналізу автомобільного трафіку в режимі реального часу. Система зчитує відеопотік, ідентифікує транспортні засоби, присвоює їм унікальні ID, визначає колір кузова та розпізнає номерні знаки.

## 📋 Функціонал
* **Постійний аналіз:** Безперервна обробка кадрів з RTSP-камери.
* **Детекція та трекінг:** Виявлення нових авто та присвоєння їм унікальних ID за допомогою ByteTrack.
* **Розпізнавання номерів (LPR):** Локалізація номерного знака на авто та зчитування тексту за допомогою EasyOCR.
* **Визначення кольору:** Аналіз середнього кольору (Mean Color) в області транспортного засобу.
* **Оптимізація:** Візуалізація результатів з рамками, колір яких відповідає реальному кольору машини.

## 🛠 Технологічний стек та вибір підходів

### 1. Моделі виявлення (YOLO)
* **Вибір:** Використано комбінацію YOLO11 (для авто) та YOLOv8 (для номерних знаків) у версіях "Nano".
* **Причина:** Це забезпечує максимальну швидкість обробки на Edge-пристроях та системах з обмеженими ресурсами.

### 2. Зчитування тексту (EasyOCR)
* **Підхід:** Текст зчитується лише з області, де знайдено номерний знак.
* **Кешування:** Для кожного ID зберігається результат з найвищим показником впевненості (confidence). Це дозволяє уникнути помилок при розмитті кадру або невдалому ракурсі. Це напевно найслабкіша частина програми - не хотів сильно перевантажувати GPU для тестового завдання.

### 3. Визначення кольору
* **Підхід:** Розрахунок середнього значення пікселів (avg_color) в межах знайденого об'єкта.
* **Причина:** Це найшвидший метод, який не потребує запуску додаткової нейромережі для класифікації кольору, що критично для реального часу. Ця частина програми залежить від більш точної постановки задачі.


## ⚙️ Інструкція із запуску

### 1. Встановлення залежностей
Переконайтеся, що у вас встановлено Python 3.8+ та виконайте команду:
```bash
pip install -r requirements.txt
```


## Запуск програми


### Використовуйте main.py з аргументом --source для вказання камери:

```Bash
python main.py --source "rtsp://ваш_потік" --skip 0.5
```

Доступні аргументи:

*    --source: URL RTSP-потоку або шлях до відео.

*    --skip: Затримка між обробкою кадрів (секунди) для оптимізації навантаження.

*    --timeout: Тайм-аут для захоплення кадру.
